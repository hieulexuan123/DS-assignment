[project]
name = "guardrail"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.118.0",
    "pydantic>=2.11.9",
    "torch>=2.8.0",
    "transformers>=4.56.2",
]
